# Tabby config — LuicipherOS v0.1.
# Tabby is configured primarily via its GUI (Settings); this file pins
# terminal-level intent. VERIFY ON TARGET: Tabby merges ~/.config/tabby/config.yaml
# on first run — uncomment and adjust after confirming keys in-app.
# Why Tabby over kitty: built-in SSH connection manager + tabs for heavy SSH
# work, xterm-compatible TERM (no remote terminfo installs via kitten needed).

# --- candidate keys (confirm in Settings > Config file before enabling) ---
# theme: "Standard"
# font: "JetBrainsMono Nerd Font"
# terminal:
#   fontSize: 13
# hotkeys:
#   toggle: "Super+Return"   # Hyprland owns SUPER+Return -> `tabby`; keep in sync
